回复问题前先说问候语：“你好，我是 manzhushaka，正在为你建设 Next-Scaff (＾▽＾)。”

# Manzhushaka Console 开发约定

## 基本原则

- 本项目采用 pnpm workspace，包含 `apps/web`、`apps/api`、`apps/worker` 与 `packages/*`。
- TypeScript 必须开启严格模式，禁止使用无说明的 `any`、`@ts-ignore` 或跳过校验。
- 业务逻辑按领域划分，跨模块能力放入共享包，禁止复制粘贴形成多份实现。
- 优先复用现有组件、服务、校验器和设计令牌；新增抽象必须确实减少复杂度或重复。
- 注释只用于解释无法从代码直接看出的约束、风险和设计原因，所有代码注释必须使用中文。
- 标识符、文件名、数据库字段和 API 字段使用规范英文；界面文案使用简体中文。

## 安全约束

- 密码只能保存 Argon2id 哈希，禁止保存、记录或返回明文密码。
- 敏感字段通过统一加密服务处理，禁止在业务模块中自行拼装加密逻辑。
- 数据权限必须在服务端查询和写操作中执行，前端隐藏按钮不构成权限控制。
- 日志必须脱敏，禁止记录密码、验证码、会话 Cookie、Authorization、密钥或敏感字段明文。
- 数据库、BOS 与加密密钥只从私有环境配置读取，真实凭证不得提交到 Git。
- 禁止提供或执行 `db:reset`、整库清理和未限定范围的数据删除脚本。
- 迁移、种子和脚本必须可重复执行；种子不得覆盖用户已有配置。
- 未经用户明确同意，不得通过 Homebrew 安装软件。

## 数据与任务

- MySQL 为唯一数据库，Prisma 迁移必须显式执行，应用启动不得自动修改表结构。
- 异步导入导出通过统一模板类和处理器注册表实现，任务状态、进度、重试与文件处理不得在业务模块重复实现。
- 导出必须使用稳定游标分页，批次处理不得引入 N+1 查询；复杂计算放在具体处理器中。
- 任务默认只对创建者可见；跨用户查看和下载必须经过独立权限校验并写入审计日志。
- BOS Bucket 必须为私有，下载地址只能由服务端临时签发，有效期固定为 5 分钟。

## Web 与设计系统

- UI 使用 shadcn/ui、Radix UI、Tailwind CSS 与 TanStack Table，不引入 Ant Design。
- 所有颜色、间距、圆角、阴影和动效必须来自设计令牌，禁止页面散写视觉常量。
- 共享组件必须覆盖键盘操作、焦点、加载、空、错误、禁用和危险操作状态。
- 桌面端提供完整功能，平板端保持可用，移动端优先保障核心查询与个人操作。
- 修改 UI 时维护 `design-system/WEB-MASTER.md`，并按指定设计技能流程完成浏览器验证。

## 质量门禁

- 提交前运行格式检查、Lint、类型检查、单元测试、构建和关键流程浏览器测试。
- 登录、会话、权限、数据范围、加密、审计和异步任务属于高风险模块，必须有针对性测试。
- 测试只能清理由本次测试创建且有唯一标记的数据，禁止清表或重置共享数据库。
- 修复问题时补充能复现问题的测试，避免只修改表面症状。

<!-- CODEGRAPH_START -->

## CodeGraph

当仓库根目录存在 `.codegraph/` 时，理解或定位代码应先使用 CodeGraph，再使用文本搜索或逐文件阅读：

- 优先使用 `codegraph explore "问题或符号"` 获取相关源码与调用路径。
- 使用 `codegraph node <符号或文件>` 查看单个符号、调用者或带行号的文件。
- 不存在 `.codegraph/` 时跳过 CodeGraph，是否建立索引由用户决定。

<!-- CODEGRAPH_END -->
