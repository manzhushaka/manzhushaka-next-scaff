# Files outside the default build output that a project-specific release script
# consumes during packaging or deployment. Keep required Nginx, migration, and
# deployment files here so a missing source file fails before upload.
# Example: deploy/example.locations.conf
deploy/manzhushaka-next-scaff.locations.conf
prisma/schema.prisma
prisma/migrations
